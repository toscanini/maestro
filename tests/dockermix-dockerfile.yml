containers:
  test_server_1: 
    base_image: ubuntu
    command: ps aux
    dockerfile: |
      FROM ubuntu
      MAINTAINER Kimbro Staken "kstaken@kstaken.com"

      RUN apt-get update
      RUN apt-get -y install bash
      CMD = ["/bin/bash"]   

  test_server_2: 
    base_image: ubuntu
    command: 'ls -l'