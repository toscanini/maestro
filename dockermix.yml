containers:
  test_server_1: 
    base_image: ubuntu
    ports: 
      - '8080'  
  test_server_2: 
    base_image: ubuntu
    dockerfile: |
      FROM ubuntu
      MAINTAINER Kimbro Staken "kstaken@kstaken.com"

      RUN apt-get update
      RUN apt-get -y install mysql-server
      CMD = ["mysqld"]   